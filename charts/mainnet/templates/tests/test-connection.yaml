apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blockchain-explorer.fullname" . }}-test-connection"
  labels:
    {{- include "blockchain-explorer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blockchain-explorer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
